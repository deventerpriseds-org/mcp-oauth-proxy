name: Make repo public (one-off)

# The CCR session's GitHub proxy blocks repo-settings writes (PATCH /repos/{owner}/{repo}), so this
# runs as a real GitHub Actions job instead, using the org's GHUB_KEY PAT (already used by
# eds-claude-skills' create-repo.yml for the same reason — repo/org admin calls the CCR proxy blocks).
# This repo needs to be public so its GHCR image (ghcr.io/deventerpriseds-org/mcp-oauth-proxy) can be
# pulled by Azure Container Apps without registry credentials, same as the other public images
# (crystaldba/postgres-mcp, the upstream obot image) already used by deploy-pg-mcp.yml. Run once;
# the repo stays public afterward, no need to re-run on future pushes.

on:
  workflow_dispatch: {}

jobs:
  make-public:
    runs-on: ubuntu-latest
    permissions:
      packages: write
    steps:
      - name: PATCH repo visibility to public
        env:
          GHUB_KEY: ${{ secrets.GHUB_KEY }}
        run: |
          set -euo pipefail
          RESP=$(curl -s -w '\n%{http_code}' -X PATCH \
            -H "Authorization: Bearer $GHUB_KEY" \
            -H "Accept: application/vnd.github+json" \
            "https://api.github.com/repos/${{ github.repository }}" \
            -d '{"private": false}')
          BODY=$(echo "$RESP" | head -n -1)
          CODE=$(echo "$RESP" | tail -n1)
          echo "$BODY"
          if [ "$CODE" -ge 300 ]; then
            echo "::error::PATCH failed with HTTP $CODE"
            exit 1
          fi
          echo "Repo is now public (HTTP $CODE)."

      # A package already published while the repo was private keeps its own visibility flag —
      # flipping the repo doesn't retroactively flip existing package versions. Patch it directly too
      # so ACA can pull ghcr.io/deventerpriseds-org/mcp-oauth-proxy without registry credentials.
      - name: PATCH package visibility to public
        env:
          GH_TOKEN: ${{ github.token }}
          GHUB_KEY: ${{ secrets.GHUB_KEY }}
        run: |
          set -euo pipefail
          try_patch() {
            local tok="$1" label="$2"
            RESP=$(curl -s -w '\n%{http_code}' -X PATCH \
              -H "Authorization: Bearer $tok" \
              -H "Accept: application/vnd.github+json" \
              "https://api.github.com/orgs/deventerpriseds-org/packages/container/mcp-oauth-proxy" \
              -d '{"visibility": "public"}')
            BODY=$(echo "$RESP" | head -n -1)
            CODE=$(echo "$RESP" | tail -n1)
            echo "[$label] HTTP $CODE: $BODY"
            [ "$CODE" -lt 300 ]
          }
          if try_patch "$GH_TOKEN" "GITHUB_TOKEN (packages:write)"; then
            echo "Package is now public via GITHUB_TOKEN."
          elif try_patch "$GHUB_KEY" "GHUB_KEY"; then
            echo "Package is now public via GHUB_KEY."
          else
            echo "::warning::Package visibility PATCH failed with both tokens — a manual visibility change in the package's GitHub settings (Package settings -> Danger Zone -> Change visibility -> Public) is needed."
          fi
